class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int n=nums.size();
        int start=0,end;
        for(start=0;start<n;start++){
            if(nums[start]!=0){
                swap(nums[start],nums[end]);
                end++;
            }
        }
    }
};

